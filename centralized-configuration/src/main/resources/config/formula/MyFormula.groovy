
def twice(x) {
    return x*2
}

def triple(x) {
    return x*3
}

def fact(x) {
    if (x > 1) return fact(x-1) * x
    else return x
}

use(DecimalCatetory) {
y = x / 3.0g
z = round(y + 34.0g, 2)
f = fact(100g).round(0)

}
